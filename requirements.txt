# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
pydantic-settings>=2.0

# AI (Gemini REST API — httpx로 직접 호출, protobuf 의존성 없음)
# google-generativeai SDK는 Python 3.14에서 protobuf C extension 충돌로 사용 불가

# Content Collection
youtube-transcript-api>=0.6.0
feedparser>=6.0.0
httpx>=0.27.0

# Database
# 2.19+ 부터 pyiceberg 의존성 추가됨 → Python 3.14에서 MSVC 없이 빌드 불가
supabase>=2.9.0,<2.19.0

# Email
resend>=2.0.0

# Templates
jinja2>=3.1.0

# Utilities
python-dotenv>=1.0.0
structlog>=24.0.0
