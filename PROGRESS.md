# DS Digest Pipeline — 개발 진행 기록

## 현재 아키텍처

```
GitHub Actions (07:30 KST)
        │
        ▼
  collect_all()
  ├── fetch_youtube_recent()     YouTube RSS → transcript / description fallback
  ├── fetch_rss_recent()         RSS 피드 (48h 필터)
  ├── fetch_arxiv_recent()       ArXiv cs.LG, stat.ML 논문 (48h 필터)
  └── fetch_hackernews_recent()  HackerNews 키워드 스토리 (24h, 점수≥50)
        │
        ▼
  _deduplicate()               Supabase seen_urls bulk 조회 + URL 정규화
        │
        ▼
  filter_and_analyze()         Gemini / Groq → relevance 점수 필터 (≥7)
        │
        ▼
  deliver()
  ├── send_telegram_digest()   텍스트 메시지 + 인라인 피드백 버튼 + tg-spoiler 퀴즈
  └── send_digest()            Resend 이메일 (HTML 템플릿)
        │
        ▼
  _mark_sent()                 발송 URL → Supabase seen_urls 기록
```

**스택**: Python 3.12 / FastAPI / Supabase / Gemini 2.5 Flash (Groq fallback) / Telegram Bot / Resend

---

## 완료된 작업

### 1. 중복 제거 강화 (`db.py`, `daily_digest.py`)
- **문제**: `is_seen()`이 Supabase 장애 시 False 반환(fail-open) + 아이템마다 N번 개별 쿼리 → 일시적 오류 한 번으로 중복 발송
- **수정**:
  - `fetch_seen_urls(urls)` 추가 — 한 번의 `IN` 쿼리로 전체 URL 일괄 조회, 실패 지점 N→1
  - `_normalize_url()` 추가 — 트레일링 슬래시·대소문자 차이로 인한 URL 미스매치 방지
  - mark_seen도 정규화된 URL로 저장

### 2. 관련도 점수 분산 (`analyzer.py`)
- **문제**: 프롬프트에 점수 정의가 없어 LLM이 보수적으로 7-8에 클러스터링
- **수정**: 1-10 각 점수의 구체적 기준 정의 + 자기 검토 지시("이 콘텐츠가 정말 9점인가?") 추가

### 3. Telegram 퀴즈 토글 (`deliverers/telegram.py`)
- **문제**: Telegram은 HTML `<details>` 미지원 → 정답이 항상 노출
- **수정**: `<tg-spoiler>` 태그로 변경 — 탭해야 정답 확인 가능한 Telegram 네이티브 스포일러

### 4. YouTube 타임라인 싱크 (`analyzer.py`)
- **문제**: LLM이 transcript 실제 타임스탬프 대신 임의로 생성(hallucination)
- **수정**: 프롬프트에 "자막에 실제로 등장한 [MM:SS]만 사용, 없으면 null" 명시

### 5. Transcript 없을 때 타임라인 처리 (`analyzer.py`)
- **문제**: transcript API 실패 시에도 프롬프트가 YouTube면 timestamp를 요구 → 100% hallucination
- **수정**: `timestamp_instruction`을 동적 생성
  - transcript 있음 → "실제 자막 시간만 사용"
  - transcript 없음 → "모든 timestamp는 null" (명시적 금지)
  - YouTube 아님 → "null"

### 6. 적용 아이디어 구체화 (`analyzer.py`)
- **문제**: "파이프라인 개선", "모니터링 도입" 같은 generic 아이디어 반복 생성
- **수정**: 3가지 조건 추가
  - 콘텐츠에서 언급된 구체적 기법/도구/알고리즘을 직접 인용할 것
  - 일반적 문장 금지
  - 구체적 기술명·지표·시나리오 포함 필수

### 7. GitHub Actions YouTube 차단 우회 (`collectors.py`, `daily_digest.yml`)
- **문제**: GitHub Actions의 datacenter IP가 YouTube에서 봇으로 차단됨
- **수정**: `YOUTUBE_COOKIES` 환경변수 지원 추가
  - Netscape 형식 쿠키 파일을 base64 인코딩 → GitHub Secret에 저장
  - `requests.Session` + `MozillaCookieJar`로 쿠키 주입 후 `http_client`로 전달
  - 쿠키 미설정 시 익명 요청으로 graceful fallback
  - **결과**: 쿠키로도 IP 차단은 우회 불가 (cloud IP 자체 차단) → description fallback으로 대응

### 8. YouTube description fallback (`collectors.py`)
- **문제**: transcript API 차단 시 YouTube 아이템의 `body`가 None → LLM이 제목만 보고 분석
- **수정**: `_extract_yt_description(entry)` 추가
  - RSS 피드 `entry.summary`에서 영상 설명글 추출, HTML 태그 제거, 3000자 truncate
  - 챕터 마커(`00:00 Intro`) 포함 시 타임라인 정보도 LLM에 전달
  - `has_body=True/False` 로깅 추가

### 9. ArXiv 논문 수집 (`collectors.py`, `config.py`)
- **추가**: `fetch_arxiv_recent(categories, hours=48)`
  - ArXiv Atom API로 `cs.LG`, `stat.ML` 카테고리 최신 논문 수집
  - abstract 전문을 body로 사용 → 분석 품질 높음
  - `ARXIV_CATEGORIES` 환경변수로 커스터마이징 가능

### 11. 아카이브 웹 뷰어 (`main.py`)
- **추가**: `GET /archive` — `data/archive/digest_*.html` 파일 목록 (최신순)
- **추가**: `GET /archive/{date}` — 특정 날짜 HTML 다이제스트 조회
  - 날짜 형식 검증 (`YYYY-MM-DD`)
  - 상단에 sticky 네비게이션 바(← 목록 링크) 자동 주입
  - 파일 없으면 404 반환

### 10. HackerNews 스토리 수집 (`collectors.py`, `config.py`)
- **추가**: `fetch_hackernews_recent(keywords, hours=24, min_score=50)`
  - Algolia HN API로 키워드 기반 스토리 수집
  - 점수 50+ 필터로 커뮤니티 검증된 콘텐츠만 포함
  - URL 기준 중복 제거, `HACKERNEWS_KEYWORDS` / `HACKERNEWS_MIN_SCORE` 설정 가능

### 11. Supabase 연동 및 중복 제거 실 검증
- Supabase 프로젝트 신규 생성 + `supabase_schema.sql` 실행
- `SUPABASE_URL`, `SUPABASE_KEY` GitHub Secrets 등록
- 실 동작 확인: `dedup_bulk_check already_seen=0 total=5` → 정상 수집 및 발송 확인

### 12. 공개 JSON 계약 (`contract.py`, `scripts/backfill_contract.py`)
- **문제**: 소비자가 읽을 안정적 표면이 없었다. `data/records/`는 내부 정본이라
  모델이 바뀌면 같이 바뀌고, HTML 아카이브는 디자인을 바꾸면 파서가 깨진다.
- **수정**: 버전 박힌 얇은 계약을 `docs/`에 발행 — `latest.json` · `{date}.json` ·
  `index.json`. 정본 35일치를 소급 발행하는 백필 스크립트 포함.
- **부수**: 발행 날짜를 KST로 고정(`today_kst`). 러너가 UTC라 한국 시각 07:10
  발행분이 전날 날짜로 저장돼 아카이브가 하루씩 밀려 있었다.

### 13. Groq llama 셧다운 대응 — 모델 교체가 아니라 출력 강제
- **문제**: 랭킹 전용 모델 `llama-3.3-70b-versatile`이 2026-08-16 셧다운됐다
  (2026-06-17 공지). 같은 공지에서 `llama-3.1-8b-instant`도 폐기돼 llama 계열엔
  대안이 없다. 폴백이 404를 흡수해 **알림 없이** Gemini → first_n으로 강등됐고,
  8/16 이후 YouTube Stage 1 선택이 사실상 랭킹 없이 돌았다.
- **수정**: `groq_ranking_model`을 분석용과 같은 `openai/gpt-oss-120b`로 합치고,
  랭킹 호출에 strict JSON Schema(constrained decoding)를 적용. llama를 쓰던 유일한
  이유(gpt-oss가 정수 인덱스 배열을 뭉갬)가 strict 모드로 사라진다 — 지원 모델이
  정확히 gpt-oss 20b/120b뿐이다. 스키마 거부(400) 시 json_object로 강등.
- **부수**: 워크플로 env에 `GROQ_RANKING_MODEL` 노출. 이게 없어서 폐기 사고 때
  시크릿으로 모델을 덮을 수 없었고, 코드 배포 없이는 복구가 불가능했다.

### 14. Telegram 아이템 발송 유실 (callback_data 64바이트 한도)
- **문제**: 버튼이 `like|{item_url}`을 실었는데 Telegram callback_data 한도는
  64바이트다. **실측 153건 중 39건(25.5%)이 초과**. 초과 시 Telegram이
  BUTTON_DATA_INVALID를 반환하고 `_send_message`가 False가 되는데, 키보드는 아이템
  메시지에 붙어 있어 **그 아이템 메시지가 통째로 발송되지 않았다**. 긴 URL을 가진
  아이템은 사용자에게 도달조차 못 했다.
- **수정**: callback_data에 12자 `item_id`(공개 계약과 동일한 식별자)를 싣고,
  수신측이 정본으로 URL을 역참조. 과거 URL 콜백도 그대로 통과한다.

### 15. 피드백 루프의 마지막 한 칸 (`preferences.py`)
- **문제**: `liked_item_ids` / `disliked_item_ids`가 **쓰기 전용**이었다. 저장까지는
  갔지만 읽는 쪽이 없어 버튼을 눌러도 다음 큐레이션이 바뀌지 않았다. 반영되던 건
  `/keyword`뿐이다.
- **수정**: 피드백 URL을 정본으로 역참조해 **출처·태그 취향**으로 일반화한 뒤
  - 상대 랭킹의 **동점 타이브레이크**로 사용 (점수는 덮어쓰지 않음)
  - Stage 1 메타 랭킹 프롬프트에 태그 힌트로 주입
- **경계**: 분석(`analyze_content`)에는 넣지 않는다. actionability·depth에 취향이
  섞이면 근거 게이트가 무의미해진다. `tests/test_evidence_gate.py`가 이를 지킨다.

### 16. Weekly Evals 복구 (`evals/build_items.py`)
- **문제**: 워크플로가 만들어진 이래 **5번 실행해 5번 전부 실패**(2026-07-27 ~
  08-24). `run.py`가 `evals/data/archive_items.json`을 읽는데 `evals/data/`는
  .gitignore 대상이고 그 파일을 만드는 스크립트도 리포에 없었다 → 매주 exit 2.
  품질 게이트가 한 번도 작동한 적이 없다.
- **수정**: 스냅샷이 없으면 커밋되는 `data/records/`에서 계측 입력을 파생.
  리포트 `input.path`에 어느 쪽을 썼는지 남긴다.
- **첫 실측(153건, 07-22~08-27)**: 태그 엔트로피 0.849→0.958, 최빈 태그 집중도
  0.887→0.170, 중복 URL 0.198→0.0, 관련도 고유값 3→5로 개선. 남은 위반은
  **YouTube 타임스탬프 0/49(0%)** 와 관련도 IQR=1.

### 17. 퀴즈 정답 수집 (`quiz_results.py`) — 루프 ⑥의 착수
- **왜 이것부터인가**: 나머지 신호는 전부 취향이라 맞고 틀림이 없다. 퀴즈만
  정답 라벨이 콘텐츠에서 파생돼 채점 가능하고, 개념별 습득도(v2 §4.2 ⑥)를
  계산할 수 있는 유일한 입력이다. 지금까지는 스포일러로 정답만 보여주고
  사용자가 맞혔는지는 버렸다.
- **설계**: 번들 퀴즈 메시지는 그대로 두고 문항별 선지 버튼 키보드를 붙인다.
  본문 문항에 전역 번호(Q1, Q2...)를 매겨 버튼 라벨(`Q1A`)과 대응시킨다.
  - 폴링이 하루 1회 **배치**라 버튼을 눌러도 콜백이 이미 만료돼 즉시 채점
    토스트를 띄울 수 없다. 그래서 정답 공개는 계속 `<tg-spoiler>`가 맡고,
    버튼은 **선택 기록** 전용이다. 채점 결과는 다음날 피드백 요약에 나온다.
  - 스포일러를 열기 전에 고른 답이 잡히므로 "맞음/틀림" 자가신고보다 라벨이
    정확하고, **어떤 오답을 골랐는지**까지 남는다 — 반복해서 같은 방향으로
    틀리면 모르는 게 아니라 잘못 알고 있는 것이라 필요한 후속이 다르다.
- **저장**: `data/quiz_results.jsonl` (append-only, 커밋). 러너가 ephemeral이라
  커밋 안 하면 매 런 사라지고, Supabase에만 두면 크레덴셜이 끊길 때 통째로
  증발한다. `data/records/`와 같은 원칙 — 로컬 JSON이 정본.
- **한계**: `accuracy_by_tag`가 습득도의 첫 근사인데, 태그가 §3.5대로 본문에
  등장한 고유명사라 매우 희소하다(`glm-5.3-flash` 같은 것). 개념 추출이
  들어오면 이 함수의 집계 키만 개념으로 바꾸면 된다.
- **부수**: `polling.py`에서 `action, token = data.split(...)` 가 봇 토큰
  파라미터를 가리고 있어 `_answer_callback`이 아이템 id를 토큰 자리에 넣어
  호출하고 있었다(예외를 삼켜 조용히 실패). `target`으로 분리했다.

### 18. 개념 어휘 (`concepts.py`) — 루프 ⑥→⑦을 잇는 잃어버린 고리
- **문제**: §3.5대로 태그는 "본문에 실제 등장한 고유명사"라 `glm-5.3-flash`,
  `artificial analysis intelligence index` 같은 1회성 문자열이 된다. 같은 값이
  두 번 나올 일이 없으니 **퀴즈 정답률도 👍 일반화도 표본 1에 머문다.**
  루프의 두 되먹임이 같은 이유로 막혀 있었다.
- **수정**: 태그보다 한 층 위의 재사용 단위를 뽑는다(§3.5 Layer 2).
  `glm-5.3-flash`는 태그로 남기고, 개념은 `모델 벤치마킹` 층위.
- **엔티티 해소**: §8.2의 3단(정규화 → 임베딩 리콜 → 회색지대 LLM) 중
  **1단만 구현하고 2·3단 대신 예방을 쓴다** — 기존 어휘를 분석 프롬프트에 넣어
  재사용을 유도한다. 사후 병합보다 애초에 파편을 덜 만드는 쪽이 싸고, 임베딩
  의존성도 임계값 두 개의 튜닝 부담도 없다.
  - 자동 병합은 정규화 키가 **완전히 일치할 때만**. §8.2의 "과다 병합이 과소
    병합보다 위험하다"를 따른 것 — 부분 일치로는 합치지 않는다.
  - 예방이 새는 만큼은 파편이 생긴다. 감시 지표가 **개념 신규율**이고,
    §8.2 기준 0.8 초과면 2·3단을 들일 시점이다. `concepts_registered` 로그에 찍힌다.
- **저장**: `data/concepts.json` (커밋). 매 런 초기화되면 모든 개념이 영원히
  "신규"가 되어 존재 의의가 사라진다.

### 19. 루프 ⑥→⑦ 연결 — 틀린 개념이 다음 큐레이션을 바꾼다
- 퀴즈 응답이 개념을 함께 기록하고(`accuracy_by_concept`), 표본 3건 이상에
  정답률 60% 미만인 개념을 `weak_concepts()`가 골라낸다.
- 그 집합이 선정 단계 타이브레이크에 **+1**로 되먹여진다. 선호 가산(+2)과
  겹쳐 쌓이므로 §4.2 ⑦의 *"습득도 낮고 선호 높은 개념"* 이 자동으로 +3을 받아
  가장 앞선다 — 조건을 따로 코딩하지 않아도 두 신호의 합에서 나온다.
- **여기서 루프가 닫힌다.** ①발송 → ②반응 → ③개념 귀속 → ④선호·습득도 →
  ⑤⑦되먹임 → ①.

### 20. 자연어 지시 (`directives.py`) — 봇에게 한국말로 말하면 다음 날 바뀐다
- **문제**: `/keyword`로 시작하지 않는 자유 텍스트를 [polling.py]가 **버리고**
  업데이트를 acknowledge까지 해서 텔레그램 서버에서도 지웠다. "논문 말고 실무
  사례 위주로" 같은 말이 흔적 없이 증발했다. `/keyword`도 "X를 더" 하나뿐이라
  "Y는 그만", "이 채널 빼줘" 같은 건 담을 수 없었다.
- **수정**: 자유 텍스트를 원문 그대로 `data/directives.jsonl`에 쌓고, 런 시작 시
  **1회** LLM으로 구조화해 두 갈래로 적용한다.

  | 필드 | 적용 | 성격 |
  |---|---|---|
  | `drop_sources` | 후보에서 제외 | 코드, 확정적 |
  | `suppress` / `boost` | 타이브레이크 ∓3 | 코드, 확정적 |
  | `standing_note` | 랭킹·분석 프롬프트 | 프롬프트, 재량적 |

  두 갈래로 나눈 이유: "arxiv 그만"은 코드로 확정해야 하고(프롬프트에 부탁하면
  지킬 때도 안 지킬 때도 있다), "논문보다 실무 사례"는 코드로 표현이 안 된다.
- **지시(∓3) > 취향(±2)**: 사용자가 직접 말한 것이 눈치로 알아낸 것보다 우선한다.
- **안전판**: `drop_sources` 적용 후 후보가 0건이 되면 **제외를 포기한다.**
  상대 랭킹 도입 목적 자체가 "빈 다이제스트를 구조적으로 없앤다"였는데 지시 한
  줄이 그걸 되돌리면 안 된다. 잘못된 지시의 최대 피해를 "한 판 어색한 큐레이션"으로
  묶고 "발송 없음"까지는 가지 않게 한다.
- **`drop_sources`는 실재하는 source_key에만 대응**시킨다. 하드 필터라 모델이
  지어낸 이름이 통하면 안 된다.
- **만료 14일 + 매일 노출**: 3개월 전 "arxiv 그만"이 영원히 살면 왜 arxiv가 안
  오는지 아무도 모르게 된다. 안 보이는 상태값이 조용히 큐레이션을 끌고 가는 것이
  이런 시스템에서 가장 위험하므로, 적용 중인 지시를 매일 Telegram으로 보낸다.
- **모순 해소**: 누적 원문을 매번 통째로 다시 해석한다. 별도 상태 파일이 없고,
  "arxiv 그만" 뒤 "역시 arxiv 살려"는 모델이 순서를 보고 정리한다.
- **한계**: 지시는 **순서를 바꾸지 공급을 만들지 못한다.** 최근 48시간에 수집되지
  않은 주제는 아무리 요청해도 나올 수 없다. 공급까지 바꾸려면 수집 설정
  (`HACKERNEWS_KEYWORDS` 등)을 건드려야 하고, 그건 잘못되면 수집이 마르므로
  이번 범위에서 뺐다.

### 21. YouTube 타임스탬프 0% — 품질 저하가 아니라 능력의 소실이었다
- **증상**: `evidence_proxy.timestamp_rate` 0/52(0%). evals FAIL 항목이자
  baseline(0.86%) 대비 회귀.
- **진단**: YouTube 52건 중 43건이 transcript를 **보유**하고 있었다
  (evidence_level=full 42건). 그런데 그 전사 43건 중 42건에 `[MM:SS]` 표기가
  **0개**였다. 분석 프롬프트는 "자막에 실제 등장한 시간만 쓰고 없으면 null"을
  정확히 지키고 있었다 — 없는 정보를 만들지 않은 것이다.
- **원인**: 구버전 youtube-transcript-api는 시간이 찍힌 세그먼트를 준다.
  datacenter IP 차단으로 Gemini 전사로 갈아타면서 그 정보가 **입력에서** 사라졌다.
  분석 단계를 아무리 조여도 없는 걸 만들 수는 없다.
- **수정**: `TRANSCRIPT_PROMPT`에 화제 전환 지점마다 `[MM:SS]`를 붙이도록 지시.
  복구는 입력 쪽에서 해야 한다.
- **미검증**: 로컬에 GEMINI_API_KEY가 없어 실제 응답으로 확인하지 못했다.
  다음 런의 `evidence_proxy.timestamp_rate`가 답을 준다.

### 22. 품질 게이트가 소음이 되지 않게
- **WARN이 잡을 떨어뜨리고 있었다**: `evaluate_thresholds`가 severity를 라벨로만
  쓰고 `exit_code = 1 if violations or regressions`가 WARN까지 셌다. "30일 이상
  미등장 소스" WARN 하나가 FAIL 두 개를 다 고쳐도 잡을 영구히 빨간불로 묶는다.
  → FAIL과 회귀만 게이트를 떨어뜨린다.
- **`| tee`가 게이트를 통째로 무력화하고 있었다**: Actions 기본 셸은 `bash -e`이고
  **pipefail이 꺼져 있다.** `run.py | tee`의 종료 코드가 tee의 0으로 덮여, exit 1을
  내도 스텝이 성공으로 잡힌다. → `set -o pipefail` 명시.
  (실측: pipefail 없음 → exit 0, 있음 → exit 1)
- **실패가 무엇을 뜻하는지 같이 보낸다**: GitHub 기본 실패 메일은 "워크플로가
  실패했다"만 알려주므로 매주 오면 곧 소음이 된다. `evals/notify.py`가 위반
  항목을 문장으로 만들어 Telegram으로 보낸다.
  본문 조립을 YAML 안 heredoc으로 인라인하려다 블록 스칼라 들여쓰기가 깨져
  워크플로가 파싱 불가가 됐다 — 스크립트로 뺐고, 그래서 테스트도 붙는다.

### 23. 관련도 점수 중앙 몰림 — 상대 비교 단계 도입 (항목 D)
- **측정이 먼저**: 투영식(A×0.6+D×0.4)이 뭉개는 줄 알았으나 아니었다.

  | 축 | 고유값 | IQR | 분포 |
  |---|---|---|---|
  | actionability | 6 | 1.00 | 73%가 5~6 |
  | depth | 6 | 1.00 | 74%가 5~6 |
  | relevance | 5 | 1.00 | — |

  **LLM이 두 축 모두 5~6점만 준다.** 투영식은 결백하고, 근거 게이트도 원인이
  아니다(164건 중 150건이 `full`이라 depth가 캡에 안 걸린다).
- **왜 프롬프트로 안 되는가**: PROGRESS 항목 2에서 이미 점수 정의 + 자기검토
  지시를 넣었는데도 이 상태다. v2 §1.1 "프롬프트로 고치려던 것이 전부 실패"의
  또 한 사례다. 구조를 바꿔야 한다.
- **수정**: 분석 후 후보 **전체를 한 목록에 놓고 서로 비교**시켜 1~10점을 다시
  받는다(`apply_relative_rating`). LLM은 절대 평가보다 상대 비교에서 안정적이다.
  - 혼합: `절대 0.4 + 상대 0.6`. 상대를 0으로 두면 변별이 없고, 절대를 0으로
    두면 후보가 전부 약한 날에도 1등이 9점을 받아 "오늘은 건질 게 없었다"는
    정보가 사라진다. 절대가 천장을 잡고 상대가 순서를 만든다.
  - **근거 천장 재적용**: 상대 평가가 근거 게이트(§3.3)를 우회하면 제목만 본
    아이템이 전사 확보 아이템을 이긴다. 혼합 결과에 근거 수준별 상한을 다시
    씌운다 (full 10 / partial 8 / description 7 / title_only 6).
  - 실패해도 파이프라인은 안 멈춘다. 호출·파싱이 깨지면 절대 점수가 그대로
    남고, 일부 index만 오면 온 것만 반영된다.
- **시뮬레이션**(실데이터 164건, 모델이 분포 규칙을 지켰다고 가정):
  `IQR 1.00 → 2.00`, 고유값 `5 → 7`, 범위 `4~8 → 3~9`. 게이트 기준 1.5를 넘는다.
- **⚠️ 가정에 의존한다**: "최고-최저 4점 이상 벌려라"는 결국 프롬프트 지시라
  보장이 아니다. 절대 채점보다 구조적으로 쉬운 과제(전체가 한눈에 보이는 비교)
  이고 strict 스키마로 형식은 강제하지만, **모델이 실제로 벌려주는지는 실측으로만
  안다.** `relative_rating_spread` 로그의 `obeyed` 필드가 그 증거다.
- **예비 점검이 잡은 회귀**: 처음엔 바닥값(`relevance_floor`)을 혼합 점수에
  걸었는데, 그러면 **상대 평가가 순서를 정하는 게 아니라 탈락을 시킨다.**
  후보가 전부 약한 날엔 모델이 시킨 대로 낮은 점수를 뿌리므로 대량 탈락이
  일어나 다이제스트가 짧아진다 — §3.4가 상대 랭킹을 도입한 목적과 정면 충돌.
  → 바닥값은 **혼합 전 절대 점수**에 걸고 정렬만 혼합 점수로 한다.
  (되계산이 아니라 스냅샷을 쓴다. relevance_score가 늘 actionability·depth에서
   파생된다는 불변식은 어디서도 강제되지 않는다.)
- **Goodhart 주의**: 이 단계는 게이트가 재는 바로 그 지표를 움직인다. 순위를
  고정 점수로 매핑하면(1등=9점, 2등=8점…) IQR은 원하는 값이 되지만 지표가
  아무것도 재지 않게 된다. 그래서 **코드로 강제 분산하지 않았다.** 모델이 안
  벌리면 IQR은 낮게 남고 게이트는 계속 경보한다 — 그게 게이트가 일하는 것이다.

### 24. 발송 전 예비 점검 (`scripts/preflight.py`)
- **왜**: 유닛 테스트는 대개 `_call_llm_with_fallback`을 대역으로 두므로,
  `_call_groq`의 페이로드 조립(`response_format`·strict 스키마)과 프롬프트
  `.format()`이 실행되지 않는다. 프롬프트에 포맷 키를 하나 추가하고 호출부를
  빠뜨리면 **전 아이템 분석이 실패해 다이제스트가 통째로 빈다.** 유닛 테스트는
  그걸 못 잡는다.
- **어떻게**: `httpx.AsyncClient.post`만 가짜로 바꾸고 파이프라인 코드는 전부
  실제로 태운다. 지시 해석 → 랭킹 → 분석 → 상대 평가 → 근거 천장 → 개념 추출까지
  13개 항목을 점검한다.
- **성과**: 첫 실행에서 바닥값 회귀(항목 23)를 잡았다.

### 25. 드라이런이 공개 산출물을 오염시킨 사고 — 출력 경로 격리
- **일어난 일**: `DRY_RUN=true` 실행이 `data/records/digest_{오늘}.json`과
  `docs/{오늘}.html` · `docs/{오늘}.json`을 mock 데이터로 만들었다. 그날치
  산출물이 아직 커밋 전이라 세 파일 모두 **untracked**였다.
  `git checkout -- data/ docs/`는 tracked 파일만 되돌리므로 mock이 살아남았고,
  이어진 `git add -A`가 그대로 커밋·푸시해 **GitHub Pages에 "[DRY RUN]"
  다이제스트가 약 11분간 공개**됐다.
- **왜 절차로 못 막나**: "드라이런 후 되돌리기"를 조심하는 것으로는 부족하다.
  되돌리기 명령이 untracked 파일을 다루지 않는다는 사실 자체가 함정이고,
  같은 실수가 언제든 재발한다. 애초에 실제 경로에 안 쓰면 된다.
- **수정**: 드라이런 출력을 `data/dryrun/` 아래로 전부 돌린다
  (archive · docs · records). `data/*`가 gitignore 대상이라 커밋될 수 없다.
- **회귀 테스트**: 경로 로직을 재구현하지 않고 **실제 `run_daily_digest`를**
  드라이런으로 태운 뒤 `data/records/` · `docs/`의 mtime이 그대로인지 본다.
  사고가 난 곳이 그 실제 경로 결정이었기 때문이다.

### 26. 소스 퍼널 기록 — 지표의 사각지대를 메운다 (`source_stats.py`)
- **사각지대**: `source_reach`는 소스 목록을 **발송된 아이템**에서 만든다.
  한 번도 발송 안 된 소스는 목록에 없고 "장기 미등장"으로 잡히지도 않는다.
  가장 나쁜 실패 모드(피드가 죽어 아무것도 안 나옴 / 수집은 되는데 매번 탈락)를
  구조적으로 못 본다.
- **실측 증거**: arXiv는 `ARXIV_CATEGORIES=cs.LG,stat.ML`로 매일 수집되는데
  **40일간 발송 0건**이었다. 그런데 지표는 "소스 18개 / 장기 미등장 1개"로
  정상 보고했다 — arXiv는 그 18개에 들어 있지도 않다.
- **원인**: v2 §3.6은 이 지표를 "(발송 / **수집**) 비율"로 정의했는데, 분모인
  수집량이 어디에도 기록되지 않아 실제로는 분자만 세고 있었다.
- **수정**: 런마다 소스별 퍼널 3단을 `data/source_stats.jsonl`에 남긴다.

      collected  수집기가 가져온 건수 (dedup 전)
      candidates dedup·채널캡을 통과해 분석에 들어간 건수
      delivered  최종 다이제스트에 실린 건수

  수집과 후보를 나눈 이유: 도달률 0의 원인이 갈린다. candidates가 0이면 중복
  제거에서 다 걸린 것(소스가 오래된 것만 낸다)이고, candidates는 있는데
  delivered가 0이면 채점·선정에서 밀린 것이다. 처방이 다르다.
- **게이트**: `source_funnel.starved_count > 0` → WARN. 기록이 없는 구간
  (도입 전)에는 0이라 통과한다 — 없는 데이터로 경보를 울리지 않는다.
- **남은 판단**: arXiv를 계속 볼 것인가. 논문 abstract가 actionability에서
  구조적으로 낮게 나오는 것이라면, 안 볼 거면 수집을 끄는 게 맞고 볼 거면
  채점 축을 손봐야 한다. 이제 데이터가 쌓이므로 근거를 갖고 정할 수 있다.

### 27. Discord 429 재시도 (`deliverers/discord.py`)
- **일어난 일**: 2026-09-02 첫 실전 발송에서 한 건이 429로 조용히 사라졌다.
  헤더 1 + 아이템 5 + 퀴즈 8을 연달아 쏘니 채널당 레이트리밋에 정상적으로
  걸린다. 예외가 아니라 응답이라 로그 한 줄만 남고 발송은 성공으로 끝났다.
- **왜 심각한가**: 아이템 메시지가 사라지면 `discord_messages.jsonl`에 매핑이
  없어 그 아이템의 👍👎가 **영영 귀속되지 않는다.** 피드백 루프에 구멍이 난 채로
  조용히 돈다.
- **수정**: 응답의 `retry_after`만큼 쉬고 최대 3회 재시도. 리액션(PUT)도 같은
  리밋을 공유하므로 같이 처리 — 봇이 미리 다는 1이 없으면 "카운트 2 이상 =
  사용자 입력" 판정이 어긋난다.

### 28. 퀴즈 정답 스포일러 + 문항 상한 5 (`deliverers/discord.py`, `config.py`)
- **정답 노출**: Discord Poll은 어느 선지가 정답인지 알려주지 않는다. 학습용
  퀴즈가 설문이 된 상태였다. `||정답: ② … — 해설||` 스포일러를 붙인다.
  본문만 잘라서 닫는 `||`가 잘려 정답이 열리는 일이 없게 한다.
- **문항 상한**: 실측 8문항 → 메시지 14개 연속 발송으로 채널이 퀴즈로 뒤덮이고
  레이트리밋에 걸렸다. `max_quiz_per_digest=5`. 아이템을 **번갈아** 채우므로
  상한이 걸려도 모든 아이템이 최소 한 문항은 낸다.

### 29. 자연어 지시 — "어떻게"를 "무엇을"로 번역하던 오류 (`directives.py`)
- **실측**: "1번 내용처럼 좀 더 어려운 내용이…" → boost=[오늘 등장한 개념 4개],
  has_note=False. 난이도 요구가 주제 요구로 바뀌어 **정반대로** 동작했다.
- **원인**: 프롬프트가 boost를 먼저 설명하고 개념 목록을 옆에 두는데
  standing_note는 잔여 항목으로만 설명돼, 모델이 눈앞의 목록에서 고른다.
- **수정**: 먼저 "무엇을"(주제·분야·출처 → boost/suppress/drop_sources)인지
  "어떻게"(난이도·깊이·형식·관점 → standing_note)인지 분류하게 했다.
  LLM 동작이라 유닛 테스트로는 못 잡는다 — 다음 수거 로그의 `has_note`로 확인.

### 30. 수식 표기 — Discord는 LaTeX를 렌더링하지 않는다 (`mathtext.py`, `analyzer.py`)
- **실측**: 퀴즈 선지 넷이 `∫_0^{4v(t)}(log(1/μ(B_d(t,r))))^{1/α}dr`처럼
  중괄호 위치만 다른 적분식이라 읽을 수 없었다. 이해가 아니라 받아쓰기 시험.
- **수정 (두 겹)**: ① 분석 프롬프트에 표기 규칙(LaTeX 금지, 유니코드 첨자,
  안 되면 한국어로) + "수식 세부만 다른 선지 금지". ② 발송 직전 `to_readable`
  로 남은 흔적을 정리하되 **확실히 좋아지는 변환만** 한다.
- **검토에서 잡은 회귀**: 첫 판은 `$...$`를 무조건 벗기고 모르는 `\명령`의
  백슬래시를 뗐다. 정본 44일치에 달러 가격 표기가 6일치 65곳 — `$500 million
  and $2 billion`이 `500 million and 2 billion`이 되고 `\d+`가 `d+`가 된다.
  → 안쪽이 LaTeX처럼 생긴 `$…$`만 벗기고, 목록에 없는 명령은 그대로 둔다.

### 31. "수집 0건" FAIL은 관측 30일 이상일 때만 (`source_stats.py`, `evals/thresholds.py`)
- **일어난 일**: 퍼널 2일치로 판정하니 주간·월간 발행 블로그 12곳이 "침묵"으로
  잡혀 FAIL 12건. 수집기가 깨진 게 아니라 그 주에 글이 없었던 것이다.
- **수정**: `MIN_DAYS_FOR_SILENCE=30` 미만이면 `confirmed_silent_families`를
  비운다. 기간이 모자랄 때 답은 "문제 없음"이 아니라 "판단 근거 없음"이다.
  짧은 기간 침묵은 WARN으로 눈에는 보이게 둔다. 결과 FAIL 12 → 0.
- **남은 FAIL 2건**(IQR 1, YouTube 타임스탬프 0/53)은 이전부터 알던 건이고,
  Weekly Evals는 이 둘 때문에 8/17부터 매주 실패 중이다.

### 32. 지시를 선정에 연결 — standing_note는 정렬 키가 아니었다 (`directives.py`, `analyzer.py`)
- **실측 2026-09-03**: "1번 내용처럼 좀 더 어려운 내용" 지시가 standing_note로
  정확히 해석돼 프롬프트에 들어갔는데도, 점수 8 논문 2건이 빠지고 6점 영상
  2건이 실렸다(출처 다양성 정규화 — 이건 사용자가 의도대로라고 확인). 최종
  정렬 키는 (점수, 👍취향 ±3 + boost/suppress ±3)뿐이고 자유 텍스트인
  standing_note는 어디에도 0이었다. 분석 프롬프트도 지시를 한 줄 나열만 하고
  어떻게 쓰라는 말이 없었다.
- **수정 (세 겹)**:
  1. 해석 단계에서 난이도·깊이 지시를 구조화 축 `difficulty`(harder/easier/"")
     로 따로 뽑아 `Directive.depth_bias`(±1)에 싣는다. 자유 텍스트는 정렬 키가
     될 수 없으므로 구조화가 먼저다.
  2. 점수 축: `depth_bias`가 depth 가중을 옮긴다(기본 0.4 → 어렵게 0.6, 쉽게
     0.2). "더 어렵게"는 곧 깊이 축을 더 세게 보라는 뜻이라 새 휴리스틱 없이
     기존 두 축의 배합만 바꾼다. 상대 평가 프롬프트에도 지시를 넣는다 — 점수를
     벌리는 단계에 없으면 글쓰기만 바뀌고 순위는 그대로다.
  3. 타이브레이크: depth ≥7이면 +3, ≤3이면 −3(쉽게는 반대). 5~6점이 대다수라
     중앙은 건드리지 않는다.
  4. 분석 프롬프트에 지시 활용 규칙 명시 — 요약·핵심·퀴즈의 깊이와 관점은
     지시에 맞추되 점수는 근거로만 매긴다(지시는 선정 단계가 따로 반영).
- **안 바꾼 것**: 출처 다양성 정규화. 점수 8 논문이 빠진 건 사용자가 좋다고
  했다. 지시는 그 안에서 순서를 정한다.
- **예비 점검이 잡은 회귀**: 처음엔 난이도 가중을 바닥값 판정에도 그대로 썼다.
  그러면 **지시가 순서가 아니라 자격을 바꾼다** — 실측으로 제목만 아이템이
  4점에서 3점이 되어 바닥 아래로 떨어졌고, 예비 점검의 근거 게이트 항목이
  통째로 사라졌다(후보가 3건 → 2건). 후보가 전부 얇은 날엔 그대로 빈
  다이제스트다. §23이 바닥값을 혼합 전 절대 점수에 건 것과 같은 이유다.
  → 바닥값·근거 천장은 **언제나 기본 배합**으로 재고, 난이도 가중은 정렬에만
  쓴다. 지시는 순서를 정하고 근거 게이트는 자격을 정한다.

### 33. 논문 배경·위치 (`positioning`) — 요약과 핵심만으로는 논문이 왜 여기 있는지 안 보인다
- **피드백**: "논문은 실을 거면 배경 설명과 이게 어느 위치에 있는 논문인지가
  명확했으면". 한 줄 요약은 결론만, 핵심 포인트는 세부만 담아서 이 논문이
  어떤 흐름의 어디에 있는지가 빠져 있었다.
- **수정**: 분석 출력에 `positioning`(2~3문장) 추가. 배경(어떤 문제를 왜,
  기존 접근이 어디서 막혔나)과 위치(어느 연구 흐름의 어느 지점, 무엇이 새로운가).
  논문·arXiv일 때만, 근거 없으면 null. Discord는 요약 바로 아래 📍로, 이메일·
  HTML·공개 JSON(`positioning`)에도 싣는다. 계약 형태 고정 테스트에 키 추가.

### 34. 어제 퀴즈 결과 되돌려주기 — Poll은 정답을 알려주지 않는다
- **피드백**: "퀴즈 정답은 근데 확인할 수가 없는 건가 내가?" 스포일러(§28)는
  눌러야 보이고 투표 뒤엔 다시 찾아가야 한다. 사용자 제안대로 다음 날 아침
  피드백 처리 메시지에 문항별 채점을 붙인다.
- **수정**: `record_answer`가 파일에는 라벨만 남기고 호출부에는 문항·내 답·
  정답·해설 원문을 얹어 준다(정본에 있으므로 중복 저장 안 함). 폴링이
  `quiz_details`로 모으고, `format_quiz_recap`이 "❌ 제목 / Q / 내 답 ③ → 정답
  ② / 해설" 형태로 만들어 2000자 경계에서 문항 단위로 나눠 보낸다.
- **주의**: 같은 👍가 최근 50개 메시지 안에 있는 동안 매일 재집계된다.
  프로필은 중복을 막지만 Supabase feedback 테이블엔 매일 행이 쌓인다(감사
  로그라 실해 없음). `digest_records` 테이블이 Supabase에 없어 매 런 경고가
  난다(§11 스키마에 없음) — 정본은 파일이라 영향 없음.

### 35. 개념 파편 — 쉼표로 이어 온 개념이 세 번째 개념이 됐다 (`concepts.py`)
- **실측 2026-09-02**: 어휘에 `멀티프로세싱, 병렬 처리`(×1)가 `멀티프로세싱`(×2)·
  `병렬 처리`(×3)와 **나란히 별도 표준 개념**으로 등록돼 있었다. 모델이 개념
  둘을 한 문자열로 냈고 등록 단계가 그대로 받았다.
- **왜 중요한가**: 루프 ③(개념 귀속)이 이 파편으로 반응을 귀속시키면, 다시
  등장하지 않을 개념에 신호가 쌓여 **조용히 사라진다.** URL 기반으로는 얻을 수
  없는 일반화를 얻으려고 개념 층을 만든 건데, 파편이 그 일반화를 되돌린다.
- **수정**: 등록·분석 파싱 양쪽에서 쉼표로만 나눈다. `/`는 `A/B 테스트`를
  쪼개고 `·`·`및`은 한국어의 정당한 연결어라 건드리지 않는다 — §8.2의
  "과다 병합보다 과소 병합"이 분할에도 그대로 적용된다.
- **함정**: 나누면 개수가 늘어 `concepts`의 상한 3을 넘길 수 있고, 그러면
  pydantic이 거부해 **그 아이템 분석이 통째로 실패**한다. 나눈 뒤 상한을
  다시 건다.
- **데이터 정리**: 기존 파편 1건을 두 개념에 합산하고 삭제했다(43개 표준 개념).

### 36. 품질 게이트의 계측 창이 무한히 자라고 있었다 (`evals/thresholds.py`, `evals/run.py`)
- **증상**: Weekly Evals가 2026-08-17부터 매주 FAIL 2건으로 실패했다.
  관련도 IQR 1.0(기준 1.5)과 YouTube 타임스탬프 0.018(기준 0.7)이다.
- **실측**: 둘 다 **이미 고쳐진 문제**였다.

      지표                  전체 42일    수정 이후
      관련도 IQR            1.0         2.0   (§23 상대 랭킹)
      YouTube 타임스탬프     1/55        1/1   (§21, 2026-09-01)

  9/3 전사를 확인하니 `[00:00] I am running this AI at home…`처럼 [MM:SS]가
  정확히 들어 있다. 나머지 43건은 수정 이전 전사다.
- **진짜 원인**: `run.py`가 `data/records/` 전체를 읽고 그 창이 **계속 커진다.**
  고장나 있던 구간이 영원히 분모에 남으므로 **버그를 고쳐도 지표가 회복되지
  않는다.** 파이프라인이 아니라 계측기가 고장난 상태였고, 그래서 3주간
  "게이트가 빨간불인데 뭘 고쳐야 하는지 알 수 없는" 상태가 지속됐다.
- **수정**: 임계값 규칙에 `scope`를 붙여 두 갈래로 나눈다.

      recent    지금 파이프라인이 제대로 도는가 (점수 분포·타임스탬프·스키마)
                → 최근 14일만 보고 판정
      lifetime  쌓인 자산이 건강한가 (중복 재유입·장기 미등장 소스)
                → 정의상 긴 창이 필요하므로 전체

  창의 기준점은 오늘이 아니라 **데이터의 마지막 날**이다. 파이프라인이 며칠
  멈추면 창이 통째로 비어, 정작 그 멈춤을 알려야 할 지표가 조용해진다.
- **표본 부족 처리**: 최근 창에 30건 미만이면 recent FAIL을 WARN으로 낮춘다.
  §31과 같은 원칙 — 근거가 모자랄 때의 답은 "문제 없음"이 아니라 "판단할
  근거가 없음"이다. 다만 보류가 영구 면제가 되지 않게 회귀 테스트로 묶었다.
- **리포트**: recent 지표는 전체 구간 값을 괄호로 함께 보여준다
  (`2.0 (전체 1.0)`). 최근만 나쁜지 원래 나빴는지가 한 줄에서 갈린다.
- **결과**: FAIL 2건 → 1건. 남은 타임스탬프 건은 아직 창의 대부분이 수정 이전
  전사라 진짜로 낮다. 전과 달리 **시간이 지나면 스스로 회복된다.**

---

## 다음 스텝 아이디에이션

### 🔴 버그/안정성 (즉시 가치)

~~**A. 피드백 루프 실제 작동 검증**~~ ✅ — 검증했더니 **반영되지 않고 있었다**.
저장 경로는 멀쩡했지만 읽는 쪽이 없었고(§15), 그 위에 25%의 아이템이 발송조차
되지 않고 있었다(§14). 둘 다 수정 완료.

**B. seen_urls 만료 정책 부재**
현재 seen_urls는 영구 보존. 같은 영상이 6개월 뒤 다시 올라와도 차단됨. `seen_at` 기준 N일 이후 만료 처리 필요 (Supabase scheduled function 또는 pipeline 시작 시 cleanup).

**C. 발송 실패 시 재시도 없음**
Telegram/Email 발송 실패 시 그냥 넘어감. 최소한 발송 실패 시 로그 알림(Telegram 자체로 에러 메시지)이라도 있어야 운영 가능.

---

### 🟡 품질 개선 (UX 임팩트)

**D. 관련도 점수 보정: 콘텐츠 간 상대 비교**
현재 각 아이템을 개별로 채점 → 절대 기준이 흔들림. 수집된 후보군 전체를 한 번에 넘겨서 "이 중에서 순위를 매겨라" 방식으로 변경하면 점수 분산이 강제됨. 단, 프롬프트 토큰이 대폭 증가하는 트레이드오프 있음.

**E. 요약 품질: one_line_summary가 제목 paraphrasing 수준**
"왜 이게 중요한가"를 담지 못하고 제목을 한국어로 바꾸는 수준. 프롬프트에 "제목을 반복하지 말고 이 콘텐츠가 DS 현업자에게 왜 중요한지 한 줄로" 조건 추가.

**F. 태그 일관성 부재**
같은 개념이 "MLOps", "ml-ops", "ML 운영" 등 다양한 형태로 생성됨. 허용 태그 목록(taxonomy)을 프롬프트에 포함하거나, 생성 후 유사도 기반 normalization.

---

### 🟢 기능 확장 (프로덕션 레벨)

**G. 아카이브 웹 뷰어**
현재 `data/archive/digest_YYYY-MM-DD.html`이 로컬에만 쌓임. Supabase Storage나 GitHub Pages에 자동 업로드하면 언제든 과거 다이제스트 조회 가능. 피드백 링크도 웹 기반으로 전환 가능.

~~**H. 소스 다양화: arXiv / HackerNews**~~ ✅ — `fetch_arxiv_recent`, `fetch_hackernews_recent` 추가 완료

**I. 멀티 유저 지원 구조**
현재 `user_id="default"` 하드코딩. `user_id`를 Telegram chat_id로 바꾸면 Telegram으로 구독 신청한 사람마다 독립적인 프로필·발송이 가능. 스키마 변경은 최소 (seen_urls에 user_id 컬럼 추가).

**J. 비용 추적**
Gemini API 호출 토큰 수를 로깅해두지 않아 실제 사용량 불투명. `response` 객체에서 `usageMetadata` 파싱 → Supabase에 일별 토큰 기록. 무료 tier 초과 예측 가능.

---

### 💡 실험적 아이디어

**K. 주 1회 "테마 다이제스트"**
일별 큐레이션과 별개로, 한 주 동안 수집된 아이템을 클러스터링해서 "이번 주 DS 트렌드 Top 3 테마" 형태로 주말에 발송. Gemini로 클러스터 라벨링 + 대표 아이템 선정.

**L. 퀴즈 정답률 트래킹**
Telegram 스포일러를 탭했을 때 실제로 맞혔는지 확인하는 버튼(맞음/틀림) 추가. 정답률 데이터가 쌓이면 "자주 틀리는 개념" 기반으로 복습 콘텐츠 우선 추천 가능.

**M. 콘텐츠 신선도 가중치**
현재 relevance_score만으로 정렬. 발행일이 오래될수록 패널티를 주는 freshness score 결합 (`final_score = relevance * 0.7 + freshness * 0.3`). 48h RSS 필터와 별개로 "며칠 됐냐"를 점수에 반영.

---

## 우선순위 제안

프로덕션 배포를 목표로 한다면 아래 순서를 추천:

1. ~~**C (발송 실패 알림)**~~ ✅ — `_send_error_alert()` 추가, 수집 0건·필터 전부·채널 실패 3개 지점에서 호출
2. ~~**B (seen_urls 30일 만료)**~~ ✅ — `cleanup_seen_urls(days=30)` 추가, 파이프라인 시작 시 자동 실행
3. ~~**A (피드백 루프 E2E 테스트)**~~ ✅ — `tests/test_feedback_loop.py` 10개 테스트 전부 통과
4. ~~**E (요약 품질 — 제목 반복 금지)**~~ ✅ — `one_line_summary` 프롬프트에 금지 조건 + 예시 추가
5. ~~**G (아카이브 웹 뷰어)**~~ ✅ — `GET /archive` 목록, `GET /archive/{date}` 상세 조회
6. **D (관련도 점수 — 상대 비교 방식)** — 점수 분산 추가 개선
7. **F (태그 taxonomy)** — 태그 일관성 확보
8. **I (멀티 유저)** — 프로덕션 스케일
